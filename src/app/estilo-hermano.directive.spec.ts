import { EstiloHermanoDirective } from './estilo-hermano.directive';

describe('EstiloHermanoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloHermanoDirective();
    expect(directive).toBeTruthy();
  });
});
